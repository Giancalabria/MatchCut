# Domain feature modules

Código de dominio versionado (no confundir con `/features` gitignored de notas locales).

- `auth/` — OAuth helpers y tipos de perfil
- `onboarding/` — región, plataformas, política de descartes
